# Claude Code 项目入口

请先读取并遵守：

@AGENTS.md

补充规则：

- 默认使用简体中文沟通。
- 不要在终端展示大段代码或完整 diff，除非用户明确要求。
- 中大型任务先按 AGENTS.md 编写 Spec 和 Plan，确认后再实现。
- `tokens/tokens.json` 是 SSOT；改视觉先改 token，再同步各平台主题。
- Typora 主题文件名必须用连字符；`clients/*` 是对外模板资产，不是本仓规范。
